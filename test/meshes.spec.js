import { Color } from 'three'
import {newMesh} from '../client/assets/models'


describe('models.js', () => {
  describe('newMesh', () => {
    it('returns a new mesh based on given inputs', ()=> {
      newMesh([1, 2, 3], )
      expect(newMesh).to.equal()
    })
  })
})